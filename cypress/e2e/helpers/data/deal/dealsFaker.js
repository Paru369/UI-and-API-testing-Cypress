import { faker } from '@faker-js/faker';
export const dealDataFaker = {
            title: `QA deal ${faker.commerce.productAdjective()}`,
            amount: `${faker.random.numeric(6)}`,
        }
        